import Foundation
import SwiftUI

struct HeaderNoProfile: View {
    let text: String
    var body: some View {
        Text(text).font(.custom("Clash Grotesk Bold", size: 22)).padding()
        
    }
}
